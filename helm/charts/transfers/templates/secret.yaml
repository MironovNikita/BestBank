apiVersion: v1
kind: Secret
metadata:
  name: transfers-service-secret
type: Opaque
stringData:
  SPRING_DATASOURCE_USER: "{{ .Values.env.SPRING_DATASOURCE_USER }}"
  SPRING_DATASOURCE_PASSWORD: "{{ .Values.env.SPRING_DATASOURCE_PASSWORD }}"
  SPRING_SECURITY_OAUTH2_SECRET: "{{ .Values.env.SPRING_SECURITY_OAUTH2_SECRET }}"