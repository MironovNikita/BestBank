image:
  repository: openzipkin/zipkin
  tag: "3.5.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9411
  targetPort: 9411

resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

replicaCount: 1

storage:
  type: mem