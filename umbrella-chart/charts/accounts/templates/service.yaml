apiVersion: v1
kind: Service
metadata:
  name: accounts-service
spec:
  type: ClusterIP
  selector:
    app: accounts-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}